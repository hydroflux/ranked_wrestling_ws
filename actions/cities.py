from actions.pages import get_page_data, get_page_handler


def count_cities(browser, league):
    page_handler = get_page_handler(browser, False)



def record_cities(browser, season, division, league, stats):
    count_cities(browser, league)